/**
 * Placeholder for the "Experiences" tab at `/experiences`.
 *
 * The nav tab is real and clickable (see Header.jsx) so the three-way
 * switch works, but this capstone hasn't built the Experiences flow yet —
 * that content is coming as its own section, same as the rest of the
 * homepage build.
 */
export default function ExperiencesPage() {
  return (
    <div className="container page">
      <div className="empty-state">
        <h1 className="empty-state__title">Experiences</h1>
        <p className="empty-state__text">This section is coming soon.</p>
      </div>
    </div>
  );
}
